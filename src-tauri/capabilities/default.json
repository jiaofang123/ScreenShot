{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "ScreenShot main and capture overlay windows",
  "windows": ["main", "overlay-*"],
  "permissions": [
    "core:default",
    "clipboard-manager:allow-write-image",
    "dialog:allow-save"
  ]
}
